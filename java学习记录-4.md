学习关于Java运算符
1. 算术运算符
    +、-、*、/、%（Java中没有地板除//，//是单行注释）

    关于+
    有数字相加  1+2
    有字符串相加 "af"+"bad"
    有字符相加 1 + 'a',这个结果为98，需要记住ASCII码表，a为97，A为65

    在进行整数除法运算时，Java是向0取整（截断），不同于Python地板除//的向下取整。

    Java与Python的不同点：运算结果不会默认变成浮点数。
    - 整数 / 整数 → 整数（小数部分直接截断）
    - 含浮点类型（float/double）的运算 → 结果为浮点类型

    当参与运算的类型不同时，如 long / int，结果会是 long 类型，这是由**类型提升**（自动类型转换）决定的。
    这个也就是隐式转换
    在计算时，类型不一样时会自动提升为同一类型再运算，小的类型转换成大的类型，byte,short,char在运算时是转换为int类型计算
    当然还有显示转换，比如 
    double a = 3.14
    int c = (int)a
    或者(byte)(b1+b2)

2. 自增自减运算符
    ++或者--
    例如a++或者a--,++或者--放在前面也是可以的++a,--a
    区别：
    int a = 10;
    int b = a++; 先用后加，b为10，a为11

    int a = 10;
    int b = ++a; 先加后用，a为11，b为11

3. 赋值运算符
    相比于python，Java没有**=（Python有**=幂赋值）和//=（地板除赋值）
    其余的和python一样
    在+=，-=，*=，/=，%=在底层都隐藏了强制转换
    short a = 1;
    a = a+1;
    原型为 a = (short)(a+1);

4. 关系运算符
    ==,!=,>,<,>=,<=
    这些也与python一样，不过底层机制稍微有点不同

5. 逻辑预算符
    非！与& 或| 异或^
    不同于Python的not,and,or
    与&，两边都为真，结果才为真
    或|，两边都为假，结果才为假
    非！，取相反的结果
    异或，相同为false，不同为true

    短路逻辑运算符
    &&短路与
    ||短路或

6. 三元运算符
    格式 
    关系表达式？表达式1：表达式2；
    true,结果为表达式1，false,结果为表达式2
    三元运算符的结果必须被使用

7. 原码，反码，补码
    ----比特(bit)最小的信息单位，取值为0或者1
    ----字节(byte)最小的存储单位,取值为8个bit

    原码，十进制数据的二进制表现形式，最左边的是符号位，0为正，1为负
    | 序号 | 记忆点                                   |
| -- | ------------------------------------- |
| 1  | 二进制就是满2进1                             |
| 2  | 1 byte = 8 bit，`byte` 范围 `-128 ~ 127` |
| 3  | 正数的原码=反码=补码，三码相同                      |
| 4  | 负数在计算机里用**补码**存                       |
| 5  | 补码 = 原码取反 + 1（符号位不变）                  |