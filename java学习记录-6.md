1. 无限循环
    while(true){        // Java中boolean是小写 true/false，不是Python的 True/False
        System.out.println("......")
    }

2. 跳转控制语句
    continue跳出本次循环，接着进行下一次循环
    break直接结束循环

3. 数组
    数组指一种容器，可以用来存储同种数据类型的多个值
    格式
    数据类型 []数组名
    括号放在变量名前或者后面都可以

    初始化就是指在内存中开辟一块空间，将数据存入的过程

    静态初始化
    数据类型[]数组名 = new 数据类型[]{元素1，元素2，元素3....};
    简化 数据类型[]数组名={元素.....};
    //如果直接打印数组会输出地址值
    
    数组索引，[],从零开始
    int []arry={1,2,3,4,5};
    int a = arry[0];
    System.out.println(a);

    System.out.println(arry[0]);    // 圆括号，不是大括号

    数组范围不能动态修改，元素可以修改
    arry[0]=100;

    关于计算数组长度，可以使用length属性和python不一样的是，python用的是len

    动态数组初始化
    数据类型[]数组名 = new 数据类型[数组长度];
    整数类型默认初始值为0
    小数为0.0
    字符类型为'/u0000' 空格
    布尔类型为false
    引用数据类型null

    只明确元素个数使用动态数组
    明确操作数据使用静态数组